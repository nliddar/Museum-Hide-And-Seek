package com.nliddar.museumhideandseek.interfaces;

public interface ExhibitRecyclerViewInterface {

    // The Activity (Class) that implements onItemClick will be able to handle on click behaviour for
    // an item in an ExhibitRecyclerView
    void onItemClick(int position);
}
